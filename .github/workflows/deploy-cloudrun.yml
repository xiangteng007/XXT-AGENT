name: Deploy Cloud Run Always-on Services + Terraform

# This workflow is currently a placeholder as Cloud Run services
# have been migrated to Firebase Functions or are not deployed.
#
# Re-enable Docker builds and Terraform when Cloud Run services are needed.

on:
  workflow_dispatch:  # Manual trigger only for now
  push:
    branches: ["main"]
    paths:
      - "services/**"
      - "infra/terraform/**"
      - ".github/workflows/deploy-cloudrun.yml"

env:
  REGION: asia-east1
  AR_REPO: ai-me-services

jobs:
  check-status:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cloud Run Status Check
        run: |
          echo "=========================================="
          echo "Cloud Run Deployment - Status Check"
          echo "=========================================="
          echo ""
          echo "This workflow is currently in maintenance mode."
          echo ""
          echo "Cloud Run services defined in this repo have been:"
          echo "  - Migrated to Firebase Functions (news-collector)"
          echo "  - Not yet deployed (other services)"
          echo ""
          echo "To deploy Cloud Run services:"
          echo "  1. Update infra/terraform/*.tf with valid resource definitions"
          echo "  2. Ensure GCP project (xxt-agent) has required APIs enabled"
          echo "  3. Build and push Docker images to Artifact Registry"
          echo "  4. Run terraform apply"
          echo ""
          echo "Active deployment workflows:"
          echo "  - Deploy to Firebase (apps/functions)"
          echo "  - Deploy to Vercel (apps/dashboard)"
          echo ""
          echo "=========================================="
          echo "Workflow completed successfully (no-op)"
          echo "=========================================="
